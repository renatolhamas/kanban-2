# F4.plan.008 â€” Auth-Aware Layout (Sidebar + UserMenu)

**Status:** ✅ CONCLUÍDO
**Data:** 2026-04-17
**Prioridade:** CRÃTICA

---

## Contexto

A `Sidebar` e o `UserMenu` aparecem para qualquer visitante â€” mesmo sem estar logado.
A lÃ³gica atual esconde esses elementos apenas em rotas de autenticaÃ§Ã£o (`/login`, `/register`, etc.),
mas **nÃ£o verifica se hÃ¡ um usuÃ¡rio autenticado de fato**.

O resultado visÃ­vel na screenshot: usuÃ¡rio nÃ£o logado vÃª a Sidebar e o UserMenu na tela home.

---

## Causa Raiz

| Arquivo | Problema |
|---|---|
| `src/components/layout/RootLayout.tsx` | Sidebar visÃ­vel em qualquer rota nÃ£o-auth, independente de sessÃ£o |
| `src/components/layout/Header.tsx` | UserMenu visÃ­vel em qualquer rota nÃ£o-auth, independente de sessÃ£o |
| `src/context/AuthContext.tsx` | **NÃƒO EXISTE** â€” planejado na arquitetura, nunca implementado |
| `src/hooks/useAuth.ts` | **NÃƒO EXISTE** â€” planejado na arquitetura, nunca implementado |

---

## SoluÃ§Ã£o

### Passo 1 â€” Criar `AuthContext` + `useAuth`

**Arquivo:** `src/context/AuthContext.tsx`

- Chama `GET /api/auth/me` ao montar (endpoint jÃ¡ existe e retorna `{ authenticated: bool, sub: string }`)
- ExpÃµe `{ isAuthenticated, isLoading, user }` via context
- Enquanto carrega: `isLoading = true` (evita flash de conteÃºdo errado)

```tsx
// Contrato pÃºblico
interface AuthContextValue {
  isAuthenticated: boolean;
  isLoading: boolean;
  user: { sub: string } | null;
}
```

**Arquivo:** `src/hooks/useAuth.ts`

```ts
export function useAuth(): AuthContextValue {
  return useContext(AuthContext);
}
```

---

### Passo 2 â€” Envolver app com `AuthProvider`

**Arquivo:** `app/layout.tsx`

```tsx
<ToastProvider>
  <AuthProvider>          {/* â† NOVO */}
    <RootLayout>
      {children}
    </RootLayout>
  </AuthProvider>
</ToastProvider>
```

---

### Passo 3 â€” Atualizar `RootLayout`

**Arquivo:** `src/components/layout/RootLayout.tsx`

```tsx
const { isAuthenticated, isLoading } = useAuth();
const isAuthPage = AUTH_PAGES.some((p) => pathname.startsWith(p));

// Sidebar: apenas usuÃ¡rio autenticado + fora de auth pages
{!isAuthPage && !isLoading && isAuthenticated && <Sidebar />}
```

---

### Passo 4 â€” Atualizar `Header`

**Arquivo:** `src/components/layout/Header.tsx`

```tsx
const { isAuthenticated, isLoading } = useAuth();

// UserMenu: apenas usuÃ¡rio autenticado
{!isAuthPage && !isLoading && isAuthenticated && <UserMenu />}
```

---

## Arquivos a Modificar

| Arquivo | AÃ§Ã£o |
|---|---|
| `src/context/AuthContext.tsx` | CRIAR |
| `src/hooks/useAuth.ts` | CRIAR |
| `app/layout.tsx` | Adicionar `<AuthProvider>` |
| `src/components/layout/RootLayout.tsx` | Trocar `!isAuthPage` por `!isAuthPage && isAuthenticated` |
| `src/components/layout/Header.tsx` | Trocar `!isAuthPage` por `!isAuthPage && isAuthenticated` |

---

## Comportamento Esperado

| SituaÃ§Ã£o | Sidebar | UserMenu |
|---|---|---|
| NÃ£o autenticado (qualquer rota) | âŒ Oculto | âŒ Oculto |
| Autenticado + rota protegida | âœ… VisÃ­vel | âœ… VisÃ­vel |
| Autenticado + rota de auth (/login) | âŒ Oculto | âŒ Oculto |
| Carregando (verificando sessÃ£o) | âŒ Oculto | âŒ Oculto |

---

---

## Bug CrÃ­tico Relacionado â€” Logout Quebrado

### Problema

O botÃ£o Logout em `UserMenu.tsx` (linha 61) faz:
```ts
localStorage.removeItem('auth_token');  // â† errado: token nÃ£o estÃ¡ no localStorage
router.push('/login');
```

O token real Ã© um **httpOnly cookie** chamado `auth_token` â€” nÃ£o acessÃ­vel via JavaScript/localStorage.
A API `POST /api/auth/logout` (`app/api/auth/logout/route.ts`) existe e faz o correto (limpa o cookie),
**mas nunca Ã© chamada**.

### ConsequÃªncia

1. UsuÃ¡rio clica Logout â†’ localStorage limpo (sem efeito real), redireciona para `/login`
2. Cookie `auth_token` ainda existe â†’ middleware detecta sessÃ£o ativa
3. Middleware redireciona de `/login` para `/` â†’ parece que logou sozinho

### Fix (incluir no Passo 1)

**Arquivo:** `src/components/layout/UserMenu.tsx` â€” `handleLogout` (linha 58-66)

```ts
const handleLogout = async () => {
  try {
    await fetch('/api/auth/logout', { method: 'POST' });
    router.push('/login');
  } catch (error) {
    console.error('Logout failed:', error);
  }
};
```

---

## VerificaÃ§Ã£o

1. `npm run typecheck` â€” 0 errors
2. `npm run build` â€” Compiled successfully
3. Acessar `http://localhost:3017` sem login â†’ Sidebar e UserMenu **nÃ£o aparecem**
4. Fazer login â†’ Sidebar e UserMenu **aparecem**
5. Fazer logout â†’ Sidebar e UserMenu **somem**
6. Toggle dark mode â†’ continua funcionando

---

## âœ… UPDATE (2026-04-17)

**O problema foi efetivamente resolvido apenas com a execuÃ§Ã£o do F4.plan.009.**

F4.plan.008 planejou a implementaÃ§Ã£o de `AuthContext` + verificaÃ§Ãµes de `isAuthenticated`, porÃ©m a execuÃ§Ã£o completa do problema (Sidebar e UserMenu aparecerem imediatamente apÃ³s login, sem necessidade de F5) foi resolvida apenas com F4.plan.009, que implementou as aÃ§Ãµes `login()` e `logout()` no AuthContext com state update sÃ­ncrono.
