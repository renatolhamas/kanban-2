# F4.plan.009 â€” CorreÃ§Ã£o: Sidebar e UserMenu nÃ£o aparecem apÃ³s login

**Fase:** 4
**Plano:** 009
**Status:** âœ… CONCLUÃDO
**Data:** 2026-04-17
**Autor:** Atlas (Analyst) + Auditoria tÃ©cnica profunda

---

## 1. Contexto e MotivaÃ§Ã£o

### 1.1 Comportamento esperado

| SituaÃ§Ã£o do usuÃ¡rio | Sidebar | UserMenu |
|---|---|---|
| PÃ¡ginas pÃºblicas (login, register, etc.) | âŒ NÃ£o exibe | âŒ NÃ£o exibe |
| PÃ¡ginas autenticadas (home, profile, etc.) | âœ… Exibe | âœ… Exibe |

### 1.2 Comportamento atual (o bug)

ApÃ³s o usuÃ¡rio fazer login com sucesso, **o Sidebar e o UserMenu permanecem invisÃ­veis** mesmo nas pÃ¡ginas autenticadas â€” como se o usuÃ¡rio ainda nÃ£o estivesse logado. Um `F5` (hard refresh) resolve temporariamente, o que Ã© uma pista importante sobre a causa.

---

## 2. DiagnÃ³stico TÃ©cnico â€” A Causa Raiz

### 2.1 Como o AuthContext funciona hoje

O estado de autenticaÃ§Ã£o da aplicaÃ§Ã£o Ã© gerenciado por `src/context/AuthContext.tsx`, que Ã© montado uma Ãºnica vez em `app/layout.tsx` e distribui `isAuthenticated`, `isLoading` e `user` para toda a aplicaÃ§Ã£o via React Context.

A verificaÃ§Ã£o de autenticaÃ§Ã£o acontece assim:

```tsx
// src/context/AuthContext.tsx
const [isAuthenticated, setIsAuthenticated] = useState(false); // comeÃ§a como false
const [isLoading, setIsLoading]             = useState(true);  // comeÃ§a como true
const [user, setUser]                       = useState(null);

useEffect(() => {
  async function checkAuth() {
    const response = await fetch('/api/auth/me'); // lÃª o cookie httpOnly
    const data = await response.json();
    if (data.authenticated) {
      setIsAuthenticated(true);
      setUser({ sub: data.sub });
    } else {
      setIsAuthenticated(false);
      setUser(null);
    }
    // finally: setIsLoading(false)
  }
  checkAuth();
}, []); // â† ARRAY VAZIO: roda apenas UMA vez, no mount inicial
```

**O `useEffect` com `[]` roda somente quando o `AuthProvider` Ã© montado pela primeira vez.**

### 2.2 Como o login funciona hoje

```
1. UsuÃ¡rio preenche o formulÃ¡rio e clica "Entrar"
2. POST /api/auth/login
   â†’ Supabase signInWithPassword
   â†’ Gera JWT customizado (HS256)
   â†’ Seta cookie: auth_token=<jwt>; HttpOnly; SameSite=Lax; Max-Age=3600
   â†’ Retorna { success: true }

3. Login page recebe { success: true }
   â†’ Chama router.push('/profile')   â† NAVEGAÃ‡ÃƒO CLIENT-SIDE

4. Next.js troca a pÃ¡gina para /profile SEM recarregar o browser
   â†’ O AuthProvider NÃƒO Ã© desmontado
   â†’ O AuthProvider NÃƒO Ã© remontado
   â†’ O useEffect([], checkAuth) NÃƒO roda novamente

5. /profile renderiza
   â†’ isAuthenticated continua false (valor stale do mount inicial)
   â†’ isLoading jÃ¡ Ã© false (terminou no mount inicial)
   â†’ Sidebar: condiÃ§Ã£o (!isAuthPage && !isLoading && isAuthenticated) â†’ false â†’ NÃƒO renderiza
   â†’ UserMenu: mesma condiÃ§Ã£o â†’ false â†’ NÃƒO renderiza
```

### 2.3 Por que o conteÃºdo da pÃ¡gina funciona mas o layout nÃ£o

As pÃ¡ginas como `/profile` **nÃ£o consomem o `useAuth()` hook**. Elas fazem fetch direto para `/api/auth/profile` com o cookie httpOnly presente no browser. Por isso, o conteÃºdo da pÃ¡gina carrega corretamente â€” o cookie estÃ¡ lÃ¡, vÃ¡lido. Mas o Sidebar e o UserMenu vivem no layout (`RootLayout` e `Header`) e dependem exclusivamente do `AuthContext`, que estÃ¡ com estado desatualizado.

### 2.4 Por que o F5 "resolve"

Um hard refresh forÃ§a o browser a recarregar completamente a pÃ¡gina. O `AuthProvider` Ã© desmontado e remontado do zero. O `useEffect([], checkAuth)` roda novamente â€” desta vez com o cookie jÃ¡ presente â€” e `GET /api/auth/me` retorna `{ authenticated: true }`. O estado Ã© atualizado corretamente e os elementos aparecem.

### 2.5 Problema secundÃ¡rio: AUTH_PAGES inconsistente

Existem **duas listas separadas** de pÃ¡ginas pÃºblicas no cÃ³digo, e elas sÃ£o diferentes:

| PÃ¡gina | RootLayout.tsx | Header.tsx |
|---|---|---|
| `/login` | âœ… listada | âœ… listada |
| `/register` | âœ… listada | âœ… listada |
| `/forgot-password` | âœ… listada | âœ… listada |
| `/reset-password` | âœ… listada | âœ… listada |
| `/resend-confirmation` | âŒ **ausente** | âœ… listada |
| `/change-password` | âŒ **ausente** | âœ… listada |

ConsequÃªncia: em `/resend-confirmation` e `/change-password`, o Sidebar **apareceria** (pois RootLayout nÃ£o considera essas pÃ¡ginas como auth), mas o UserMenu **nÃ£o** (Header as exclui). Comportamento split-brain e inconsistente.

---

## 3. Mapa de Arquivos Envolvidos

| Arquivo | Papel |
|---|---|
| `src/context/AuthContext.tsx` | Fonte da verdade do estado de auth â€” **precisa expor aÃ§Ã£o de login** |
| `src/hooks/useAuth.ts` | Re-export do hook â€” pode precisar de atualizaÃ§Ã£o de tipos |
| `app/(auth)/login/page.tsx` | PÃ¡gina de login â€” **precisa chamar a aÃ§Ã£o antes de navegar** |
| `src/components/layout/RootLayout.tsx` | Renderiza Sidebar condicionalmente â€” **AUTH_PAGES precisa ser corrigido** |
| `src/components/layout/Header.tsx` | Renderiza UserMenu condicionalmente â€” **fonte do AUTH_PAGES completo** |

---

## 4. O Que Precisa Ser Feito

### 4.1 SoluÃ§Ã£o escolhida: expor uma aÃ§Ã£o `login()` no AuthContext

Esta Ã© a abordagem mais limpa e correta. Em vez de depender de navegaÃ§Ã£o hard ou de um `useEffect` com dependÃªncia de pathname, o `AuthContext` expÃµe uma funÃ§Ã£o `login(user)` que atualiza o estado diretamente e de forma sÃ­ncrona. A pÃ¡gina de login chama essa funÃ§Ã£o apÃ³s o sucesso do POST, antes de navegar.

**Vantagens:**
- Estado atualizado antes da navegaÃ§Ã£o â†’ componentes de layout renderizam corretamente na primeira exibiÃ§Ã£o
- Sem hard refresh, sem recarregamento desnecessÃ¡rio
- PadrÃ£o React idiomÃ¡tico
- NÃ£o cria dependÃªncias de pathname no useEffect (o que causaria chamadas excessivas Ã  API)

### 4.2 MudanÃ§as necessÃ¡rias

#### MudanÃ§a 1 â€” `src/context/AuthContext.tsx`

Adicionar uma funÃ§Ã£o `login(userData)` ao contexto que atualiza `isAuthenticated` e `user` diretamente:

```tsx
// Adicionar ao AuthContextValue (interface/type do contexto):
login: (user: { sub: string; email?: string }) => void;
logout: () => void; // opcional, se nÃ£o existir

// ImplementaÃ§Ã£o:
const login = useCallback((userData: { sub: string; email?: string }) => {
  setIsAuthenticated(true);
  setUser(userData);
}, []);

// Expor via context value:
value={{ isAuthenticated, isLoading, user, login }}
```

#### MudanÃ§a 2 â€” `app/(auth)/login/page.tsx`

ApÃ³s receber `{ success: true }` da API, chamar `login()` antes de navegar:

```tsx
// Antes (atual):
router.push('/profile');

// Depois (corrigido):
const { login } = useAuth();
// ...
if (data.success) {
  login({ sub: data.sub, email: formData.email }); // atualiza contexto imediatamente
  router.push('/profile'); // navega com estado jÃ¡ correto
}
```

> **Nota:** A API de login precisarÃ¡ retornar o `sub` (userId) no body de resposta para que a pÃ¡gina de login possa passÃ¡-lo ao contexto. Verificar se jÃ¡ retorna ou se precisa ser adicionado ao response body.

#### MudanÃ§a 3 â€” Unificar AUTH_PAGES em um Ãºnico local

Criar uma constante compartilhada (ex: `src/lib/auth-pages.ts`) com a lista completa de pÃ¡ginas pÃºblicas, e importÃ¡-la em ambos `RootLayout.tsx` e `Header.tsx`:

```tsx
// src/lib/auth-pages.ts
export const AUTH_PAGES = [
  '/login',
  '/register',
  '/forgot-password',
  '/reset-password',
  '/resend-confirmation',
  '/change-password',
];
```

Substituir as duas listas locais por essa importaÃ§Ã£o.

---

## 5. CritÃ©rios de AceitaÃ§Ã£o

- [ ] ApÃ³s login bem-sucedido, o Sidebar aparece imediatamente na primeira pÃ¡gina autenticada, sem necessidade de F5
- [ ] ApÃ³s login bem-sucedido, o UserMenu aparece imediatamente no Header, sem necessidade de F5
- [ ] Em todas as pÃ¡ginas pÃºblicas (`/login`, `/register`, `/forgot-password`, `/reset-password`, `/resend-confirmation`, `/change-password`), o Sidebar e o UserMenu **nÃ£o** aparecem
- [ ] ApÃ³s logout, o Sidebar e o UserMenu desaparecem corretamente
- [ ] Um hard refresh em qualquer pÃ¡gina autenticada mantÃ©m o Sidebar e UserMenu visÃ­veis

---

## 6. VerificaÃ§Ã£o / Como Testar

1. Abrir o browser em modo anÃ´nimo (garantir que nÃ£o hÃ¡ cookie anterior)
2. Navegar para `/login`
3. Confirmar que Sidebar e UserMenu **nÃ£o** estÃ£o visÃ­veis
4. Fazer login com credenciais vÃ¡lidas
5. Confirmar que, **sem F5**, Sidebar e UserMenu aparecem imediatamente apÃ³s a navegaÃ§Ã£o para `/profile`
6. Navegar entre pÃ¡ginas autenticadas e confirmar persistÃªncia dos elementos
7. Fazer logout
8. Confirmar que Sidebar e UserMenu desaparecem
9. Testar `/resend-confirmation` e `/change-password` â€” confirmar ausÃªncia dos dois elementos

---

## 7. Agente ResponsÃ¡vel pela ImplementaÃ§Ã£o

**@dev (Dex)** â€” ImplementaÃ§Ã£o das 3 mudanÃ§as acima, seguindo o fluxo SDC padrÃ£o.

---

## âš ï¸ CRÃTICA: Retrabalho InaceitÃ¡vel e Vergonhoso

**Este plano representa retrabalho inaceitÃ¡vel e vergonhoso.**

- **F4.plan.005.md** jÃ¡ havia implementado a verificaÃ§Ã£o condicional por rota para ocultar Sidebar/UserMenu em pÃ¡ginas pÃºblicas
- **F4.plan.008.md** jÃ¡ havia planejado a implementaÃ§Ã£o completa de AuthContext com state management
- **F4.plan.009.md** deveria ter sido apenas a execuÃ§Ã£o do F4.plan.008, nÃ£o uma nova auditoria e replaneamento

A raiz do problema foi **falta de sincronizaÃ§Ã£o entre planos e execuÃ§Ã£o**: os dois planos anteriores jÃ¡ continham a soluÃ§Ã£o, mas nÃ£o foram executados adequadamente. Este plano Ã© evidÃªncia de falha no processo de acompanhamento e coordenaÃ§Ã£o entre fases de planejamento e implementaÃ§Ã£o.

**LiÃ§Ã£o:** Antes de criar novos planos, verificar se planos anteriores jÃ¡ cobrem o escopo.

---

*Plano gerado por Atlas (Analyst) apÃ³s auditoria tÃ©cnica profunda â€” 2026-04-17*
