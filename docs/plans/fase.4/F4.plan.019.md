---
id: F4.plan.019
title: "Fix Quality Gate Blockers & Technical Debt"
epic: 4
**Status:** ✅ CONCLUÍDO
created_at: 2026-04-20
---

# Plan 019: Fix Quality Gate Blockers & Technical Debt

**Objetivo:** Resolver os impedimentos do "Quality Gate" reportados pelo DevOps (Gage), garantindo que a build de produÃ§Ã£o passe e o repositÃ³rio mantenha a integridade de tipos e padrÃµes de cÃ³digo.

## ðŸ“‹ DiagnÃ³stico (Quality Gate Failures)

1. **TypeScript (`lib/supabase-client.ts`)**: Erro de atribuiÃ§Ã£o no cache de clientes devido a incompatibilidade de tipos inferidos.
2. **Test Failures (`lib/jwt.ts`)**: `generateJWT` estÃ¡ comentado/desativado, quebrando os testes de integraÃ§Ã£o do Evo GO QR.
3. **Lint Errors**: 2 instÃ¢ncias de `Unexpected any` (`no-explicit-any`) em `lib/jwt.ts` e `lib/middleware/auth.ts`.
4. **Git Checks**: Diversos arquivos com *trailing whitespaces* (espaÃ§os ao final da linha).

---

## ðŸ› ï¸ MudanÃ§as Propostas

### 1. CorreÃ§Ã£o do Supabase Client Factory
- **Arquivo:** `lib/supabase-client.ts`
- **AÃ§Ã£o:** Tipar explicitamente o `clientCache` para aceitar a interface base do `SupabaseClient`.
- **Risco:** Baixo (ajuste de tipagem, nÃ£o altera lÃ³gica de conexÃ£o).

### 2. RestauraÃ§Ã£o/Mock de `generateJWT` para Testes
- **Arquivo:** `lib/jwt.ts`
- **AÃ§Ã£o:** Re-habilitar a exportaÃ§Ã£o da funÃ§Ã£o `generateJWT` (necessÃ¡ria para os testes de integraÃ§Ã£o).
- **Risco:** Baixo (funÃ§Ã£o usada apenas em contextos controlados e testes).

### 3. ResoluÃ§Ã£o de Erros de Lint (Explicit Any)
- **Arquivos:** `lib/jwt.ts`, `lib/middleware/auth.ts`
- **AÃ§Ã£o:** Substituir o uso de `any` por interfaces especÃ­ficas (`JWTPayload`).
- **Risco:** Baixo (melhora a seguranÃ§a de tipos).

### 4. Limpeza de EspaÃ§os em Branco (Git Hygiene)
- **AÃ§Ã£o:** Executar correÃ§Ã£o automÃ¡tica em todos os arquivos reportados pelo DevOps.

---

## âœ… Plano de VerificaÃ§Ã£o

### Testes Automatizados
- [ ] `npm run lint` -> Deve retornar 0 erros.
- [ ] `npm run typecheck` -> Build do TypeScript deve passar sem erros.
- [ ] `npm test` -> Todos os testes devem passar (incluindo `evo-go-qr.integration.test.ts`).
- [ ] `npm run build` -> Build do Next.js deve completar com sucesso.

### ValidaÃ§Ã£o Manual
- [ ] Testar fluxo de Login (AuthContext).
- [ ] Verificar carregamento de dados no Kanban.

---

## ðŸ“… Timeline
- **ImplementaÃ§Ã£o:** ~30-60 min
- **ValidaÃ§Ã£o:** ~15 min

---
*ConcluÃ­do em: 2026-04-20*
*Dex â€” Builder Agent*