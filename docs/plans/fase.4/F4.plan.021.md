# F4.plan.021 — Unit Tests WCAG AA + Ajuste de Escopo (Story 4.2)

**Data:** 2026-04-20
**Autor:** Dex (Dev Agent)
**Story:** 4.2 — Home Page Kanban Board
**Status:** ✅ CONCLUÍDO
**Parte de:** Story 4.2 — Home Page Kanban Board (`docs/stories/4.2.story.md`)

---

## Contexto

Após a @ux-design-expert implementar os fixes de acessibilidade WCAG AA nos componentes da `/home` (Story 4.2 v3.6), o @dev foi acionado para:

1. Remover `last_message` do escopo da story (mensagens ainda não implementadas — Story 5.1 é blocking)
2. Criar unit tests para os 4 componentes kanban com cobertura de renderização, interação e acessibilidade

---

## Decisão: Last Message Preview → Excluído do Escopo

**Motivo:** A tabela `messages` ainda não é populada — Story 5.1 (webhook handler) é responsável por criar registros de mensagens a partir dos eventos do EVO GO. Implementar a subquery agora geraria código sem dados reais para testar.

**Ação tomada:**
- Seção `## Excluído do Escopo (Decisões de Sprint)` criada em `docs/stories/4.2.story.md`
- Task 3 atualizada: checkboxes de last_message substituídos por nota de exclusão
- Change Log v3.7 registrado

**Retomada:** Após Story 5.1 estar completa e `messages` populadas via webhook.

---

## Unit Tests Criados

### Framework e Setup

| Item | Valor |
|------|-------|
| Runner | Vitest v4.1.4 |
| Environment | jsdom |
| Libs | @testing-library/react, userEvent, jest-axe |
| Alias fix | `vitest.config.ts` — `@/` → `src/` (alinhado com tsconfig paths) |
| Include path adicionado | `src/components/**/*.test.tsx` |

### Arquivos Criados

| Arquivo | Testes | Resultado |
|---------|--------|-----------|
| `src/components/ui/organisms/KanbanBoard.test.tsx` | 4 | ✅ PASS |
| `src/components/ui/organisms/KanbanColumn.test.tsx` | 8 | ✅ PASS |
| `src/components/ui/molecules/ConversationCard.test.tsx` | 19 | ✅ PASS |
| `src/components/ui/molecules/KanbanSelector.test.tsx` | 7 | ✅ PASS |
| **Total** | **38** | **✅ 38/38** |

### Cobertura por Categoria

| Categoria | O que foi testado |
|-----------|------------------|
| **Rendering** | Props renderizadas corretamente, truncamento 80 chars, badge condicional |
| **Semantics** | `<section>`, `aria-label`, `aria-live`, heading hierarchy |
| **Interaction** | Click, Enter, Space, teclas inválidas, onValueChange |
| **A11y (axe)** | `toHaveNoViolations()` para todos os componentes |
| **ARIA** | `role="button"`, `tabIndex`, `aria-pressed`, `aria-hidden`, `id` |

---

## Alterações ao vitest.config.ts

1. **Alias fix:** `@/` agora resolve para `src/` (compatível com tsconfig `paths`)
2. **Include ampliado:** `src/components/**/*.test.tsx` adicionado ao projeto jsdom
3. **Limpeza:** imports não usados (`playwright`, `dirname`, `fileURLToPath`) removidos

---

## Arquivos Modificados/Criados

| Arquivo | Tipo | Descrição |
|---------|------|-----------|
| `docs/stories/4.2.story.md` | Modificado | Seção excluído do escopo + Task 3 + Change Log v3.7 |
| `vitest.config.ts` | Modificado | Alias fix + include path + limpeza |
| `src/components/ui/organisms/KanbanBoard.test.tsx` | Criado | 4 testes |
| `src/components/ui/organisms/KanbanColumn.test.tsx` | Criado | 8 testes |
| `src/components/ui/molecules/ConversationCard.test.tsx` | Criado | 19 testes |
| `src/components/ui/molecules/KanbanSelector.test.tsx` | Criado | 7 testes |

---

## Próximos Passos

1. **@qa (Quinn):** Executar QA gate formal da Story 4.2
2. **@devops (Gage):** Push das mudanças após gate aprovado
3. **Story 5.1:** Webhook handler — quando completa, retomar last_message preview
